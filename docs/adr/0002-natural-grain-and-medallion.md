# ADR 0002 — Natural-grain Silver, reusable-measurement Gold

Decision: Silver retains source-natural scientific grain. Gold may aggregate into reusable measurements but cannot encode FCV treatment/control or estimator semantics.

Reason: preserves optionality and prevents downstream experiment choices from becoming irreversible ingestion transformations.
